#
# list of keys at /usr/include/X11/keysymdef.h
#
# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

    # Use the windows key
set $mod Mod4

    # Focus follows mouse
focus_follows_mouse yes

    # How to handle annoying popups
    # popup_during_fullscreen <smart|ignore|leave_fullscreen>
    # show the popup only if it belongs to the fullscreen application
popup_during_fullscreen smart

# workspaces {{{1
#
set $WS1 "1: shell"
set $WS2 "2: web"
set $WS3 "3: misc"
set $WS4 "4: chat"
set $WS5 "5: steam"
set $WS6 "6: media"
set $WS7 "7: docs"
set $WS8 "8: misc"
set $WS9 "9: vm"
set $WS0 "10: games"

    # put the workspaces on the right screens
workspace   $WS1      output    DVI-I-2
workspace   $WS2      output    DVI-I-2
workspace   $WS3      output    DVI-I-2
workspace   $WS4      output    DVI-I-3
workspace   $WS5      output    DVI-I-3
workspace   $WS6      output    DVI-I-3
workspace   $WS7      output    DVI-I-2
workspace   $WS8      output    DVI-I-2
workspace   $WS9      output    DVI-I-3
workspace   $WS0      output    DVI-I-2
# window classes {{{2
    # bind applications to their appropriate workspaces
    # get window class with xprop
assign [class="Firefox"] → $WS2
assign [class="Opera"] → $WS2
assign [class="Midori"] → $WS2
assign [class="Dwb"] → $WS2
#assign [class="Chromium"] → $WS4
assign [class="Mumble"] → $WS5
assign [class="Steam"] → $WS5
#assign [class="Smplayer"] → $WS6
assign [class="Smtube"] → $WS6
assign [class="Mplayer"] → $WS6
assign [class="Vlc"] → $WS6
assign [class="evince"] → $WS7
assign [class="Zathura"] → $WS7
assign [class="libreoffice-writer"] → $WS7
assign [class="Xsane"] → $WS7
assign [class="Gimp"] → $WS7
#assign [class="VirtualBox"] → 9: vm
assign [class="hl2_linux"] → 10: games
assign [class="EufloriaHD.bin.x86_64"]	10: games
assign [class="csgo_linux"]	10: games
# 2}}}
# 1}}}
# styles {{{1
#
# i3bar {{{2
bar {
            # show the workspace buttons
        workspace_buttons yes
            # use a more readable font
        font pango:Consolas 10
            # show tray icons on the primary monitor
        tray_output primary
            # use our custom statusbar command
        status_command statusbar
}
# 2}}}
    # Font for window titles. Will also be used by the bar unless a different font
    # is used in the bar {} block below. ISO 10646 = Unicode
#font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
    # The font above is very space-efficient, that is, it looks good, sharp and
    # clear in small sizes. However, if you need a lot of unicode glyphs or
    # right-to-left text rendering, you should instead use pango for rendering and
    # chose a FreeType font, such as:
    # font pango:DejaVu Sans Mono 10
font pango:Consolas 10
    # Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod
    # default borders
        # normal windows get 1pixel borders only
new_window          1pixel
        # floating windows get normal borders
new_float           normal
        # hide borders adjacent to the screen edges
hide_edge_borders   both
    #class              border  backgr. text
#client.focused          #C9B5BD #C9B5BD #825969
#client.focused_inactive #191919 #191919 #999999
#client.unfocused        #191919 #191919 #999999
#client.urgent           #cd989a #cd989a #2e3436
#client.background       #121212
# }}}
# hotkeys {{{1
    # switch to workspace {{{3
bindsym $mod+1 workspace $WS1
bindsym $mod+2 workspace $WS2
bindsym $mod+3 workspace $WS3
bindsym $mod+4 workspace $WS4
bindsym $mod+5 workspace $WS5
bindsym $mod+6 workspace $WS6
bindsym $mod+7 workspace $WS7
bindsym $mod+8 workspace $WS8
bindsym $mod+9 workspace $WS9
bindsym $mod+0 workspace $WS0
	# }}}
# windows {{{2
# containers {{{3
    # focus {{{4
    # focus the parent container
bindsym $mod+a focus parent
    # focus the child container
bindsym $mod+d focus child
    # move focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right
    # kill focused window
bindsym $mod+Shift+q kill
# 4}}}
# split {{{4
    # split in horizontal orientation
bindsym $mod+shift+h     split h
bindsym $mod+shift+minus split h
    # split in vertical orientation
bindsym $mod+shift+v     split v
bindsym $mod+bar         split v
# 4}}}
	# layout {{{4
    # enter fullscreen mode for the focused container
bindsym $mod+u fullscreen
    # change container layout (stacked, tabbed, toggle split)
bindsym $mod+apostrophe layout stacking
bindsym $mod+comma      layout tabbed
bindsym $mod+period     layout toggle split
# 4}}}
	# move {{{4
    # move focused container within workspace
bindsym $mod+space+h move left
bindsym $mod+space+j move down
bindsym $mod+space+k move up
bindsym $mod+space+l move right
    # move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $WS1
bindsym $mod+Shift+2 move container to workspace $WS2
bindsym $mod+Shift+3 move container to workspace $WS3
bindsym $mod+Shift+4 move container to workspace $WS4
bindsym $mod+Shift+5 move container to workspace $WS5
bindsym $mod+Shift+6 move container to workspace $WS6
bindsym $mod+Shift+7 move container to workspace $WS7
bindsym $mod+Shift+8 move container to workspace $WS8
bindsym $mod+Shift+9 move container to workspace 9: vm
bindsym $mod+Shift+0 move container to workspace 10: games
# }}}
    # floating {{{3
bindsym $mod+Shift+space floating toggle
    # change focus between tiling / floating windows
#bindsym $mod+space focus mode_toggle
# }}}
    # session {{{3
bindsym $mod+r reload
    # restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
    # exit i3 (logs you out of your X session)
#bindsym $mod+Shift+x exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"
bindsym $mod+Shift+x exec "i3-msg exit"
# 3}}}
    # resize {{{3
bindsym $mod+p exec notify-send -u low --app-name="i3" "Entering RESIZE mode"; mode "resize"
# 3}}}
# 2}}}
# commands {{{2
    # dmenu {{{3
bindsym $mod+e exec dmenu_run
    # There also is the (new) i3-dmenu-desktop which only displays applications
    # shipping a .desktop file. It is a wrapper around dmenu, so you need that
    # installed.
    # bindsym $mod+d exec --no-startup-id i3-dmenu-desktop
# 3}}}
    # screenshot {{{3
bindsym $mod+PrintScreen exec scrot  ~/screenshot_%Y_%m_%d_%H_%M_%S.png
# 3}}}
    # password {{{3
	# ask for user input, then pass it to xkeypass
	# facilitates reading passwords from the password store
bindsym $mod+w exec i3-input -F 'exec pw %s type password'
# 3}}}
	# i3-input {{{3
	# ask for user input, then pass it to i3-msg
	# facilitates sending arbitrary commands to i3
bindsym $mod+i exec i3-input -P 'i3-msg: ' -F 'exec i3-msg %s'
# 3}}}
# 2}}}
# modes {{{1
mode "resize" {
#
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym Shift+h resize shrink width 50 px or 50 ppt
        bindsym Shift+l resize grow width 50 px or 50 ppt
        bindsym Shift+k resize shrink height 50 px or 50 ppt
        bindsym Shift+j resize grow height 50 px or 50 ppt
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt

        # back to normal: same hotkey
        bindsym $mod+p mode "default"
		bindsym Escape exec notify-send -u low --app-name="i3" "Exiting RESIZE mode"; mode "default"
}
# }}}
# for-window {{{1
# for_window[class="gnome-control-center"] floating enable
#for_window[class="Dwb"] focus child, layout tabbed, focus
##for_window[class="Pidgin" window_role="conversation"] focus child, layout tabbed, focus
#for_window[instance="Toplevel"] floating enable
#for_window[title="foobar2000.exe - Wine desktop"] 
#or_window[class="Steam" title="*Chat$"] floating enable
# 1}}}
# auto-run {{{1
#
    # auto-run these apps when starting i3
        # 1:shell
#exec --no-startup-id i3-msg 'workspace $WS1;new_window none'
#exec --no-startup-id i3-msg 'workspace $WS1;exec myterm'
#        # 2:web
#exec --no-startup-id i3-msg 'workspace $WS2;new_window none'
##exec --no-startup-id i3-msg 'workspace $WS2;exec dwb'
##exec --no-startup-id i3-msg 'workspace $WS2;exec tor-browser-en'
#exec --no-startup-id i3-msg 'workspace $WS2;workspace_layout tabbed'
#        # 4:chat
#exec --no-startup-id i3-msg 'workspace $WS4;exec myterm'
#        # 5:steam
#exec --no-startup-id i3-msg 'workspace $WS5;workspace_layout splitv'
#exec --no-startup-id i3-msg 'workspace $WS5;new_window 1pixel'
#exec --no-startup-id i3-msg 'workspace $WS5;new_float normal'
#exec --no-startup-id i3-msg 'workspace $WS5;exec mumble'
#
#exec --no-startup-id i3-msg 'workspace 1;'
# }}}

# vim: foldmethod=marker:
