#
# session preconfiguration for system monitoring
#
    #start from default
source-file ~/.config/tmux/tmux.conf

    #apply hot colorscheme
source-file ~/.config/tmux/tmux-colors-hot.conf

    #when refreshing, refresh this configuration
bind-key    R       source-file ~/.config/tmux/tmux-session-system.conf\; display-message " ✱ tmux.conf reloaded"

set-option    -s      automatic-rename    off
set-option    -s      clock-mode-style    24
set-option    -s      monitor-activity    off
